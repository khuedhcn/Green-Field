# Copy to .env and fill in. The AI analysis endpoint reads these.
# Without ANTHROPIC_API_KEY the /api/ai/analyze endpoint returns 503 and the
# UI falls back to a local summary (no crash).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
PORT=5000
